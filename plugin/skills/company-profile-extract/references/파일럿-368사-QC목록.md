# Phase B 감독 QC 목록 (아침 검토용)

## 2026-08-26 Phase 3 후속 124사 — 시작 전 기준선

`qc.py all` (호스티드 `kglymnsytrenlbodmbid.supabase.co`):
cases 39 통과 · 삼성 회귀 18블록 동일 · R&D>매출 0 · 이상 period_key 0 ·
event_ym NULL 0 · 비율[0,100]밖 0. fin_details 10,661 / corp_history 7,878
(파일럿 종료와 동일 — PR #48 64개사는 적재되지 않음).

integrity 시장점유율 24건은 아래 N 의 미조치 3사(고려아연·금강공업·대원산업).
신규 파서 결함 아님. 런북 §1 재산정: 원문 477 − 기적재 353 = **이번 124사**.
상세는 `phase3-후속-124사.md`.

## 2026-08-26 웨이브 1 (batch01–10, 100사) 후

적재: fin_details 10,661→**13,029** / 353→**439사** · corp_history 7,878→**9,939** / 342→**424사**.
물리 불가능 값(R&D>매출·이상 period_key·event_ym NULL·비율[0,100]밖) 0 유지.
integrity 시장점유율 24→25건 — 기존 N 3사 + **00145552 이수화학 클로티냅 2025A 1행 100%**.
항목명에 '당사'가 없어 스캔이 잡음. 단일 제품 100%일 수 있어 삭제하지 않음(판정 확인).

**타겟팅 함정 (코드 아님):** `extract` 가 최신 사업보고서를 고르는데, 그 zip 이
한도초과·connection reset 인 회사가 다수. 옛 `ok` 원문은 있다. 웨이브 종료 후
`--rcepts` 로 최신 `ok` 회차 재시도.

**재현·수정한 기간 라벨** (`qc.py cases` 47개 통과 · 삼성 회귀 동일):
- 받음: `제N기(YYYY년말)` · `제N기 기말 (YYYY년 12월말)` · `YYYY년 말/기말` · `YYYY.01~YYYY.12`
- 계속 거부: `YYYY.01~YYYY.09`(YTD) · `제N기 기말 (YYYY년 9월말)`

**수정:** `prepare` 에 회사·회차를 한꺼번에 넘기면 카티전곱이던 것을 1:1 `zip` 으로 바꿈.
길이 불일치는 종료 코드 2.

**재발견 (수정 없음):** 배치09가 폴백 적재 뒤 `extract` 를 다시 돌려 에이전트 블록을
지웠다가 ingest 로 복구. 런북 §5 / 아래 M 과 동일. 최종 데이터 정상.

Thousand USD 단위는 환산하지 않고 금액 확인불가 — 지어내지 않는 쪽이 맞다.

## 2026-08-26 웨이브 2 (batch11–13, 24사) 후

적재: fin_details **13,760 / 463사** · corp_history **10,552 / 448사**.
124사 중 최신 zip 실패로 0행인 곳 14사 + 그 사이 Phase 3 신규 ≈11사 → 미처리 25사.
`--rcepts` 재시도 대기.

## 2026-08-26 전회차 목표 — 최신 ok 재시도 + Phase 3 증분

목표를 "회사당 최신 1건"에서 **ok 원문이 있는 정기보고서 전회차**(기본계정·프로필 5블록·주석)로 넓힘.

측정(호스티드, `사업보고서 (` 만, 제출기한연장 제외):
- ok 연간 원문 9,545 · pending 9,093 · 회사 492 / 기적재 463
- 회사별 최신 ok 미적재 **42사** (최신 zip 실패 재시도 + Phase 3 신규)
- pending FY2015+ 5,079 · FY2020+ 2,763
- 반기 11,547 · 분기 23,259 (프로필 파서 미검증, 섹션·주석 경유)
- `fin_periods` 134,279 (2015A–2026A, 상장 2,648사 API 경로) — 기본계정은 XBRL 연도가 이미 깔림
- 주석 NOTE 행은 시작 시점 53행/2회차. 섹션 있는 최신 연간 340사 중 2사만 주석 적재

**코드:** `pending`/`latest_annual_rcept` 필터를 `*사업보고서 (*` 로 좁힘.
`사업보고서*` 가 `사업보고서제출기한연장신고서` 를 최신 회차로 고르던 함정.

진행: 42사 최신 ok 규칙 추출 42/42. 최신 pending 80 + 다음 200회차 규칙 추출 진행.
주석 120사 규칙 적재 완료(행 있는 곳 67사). 폴백 batch14–17 중 15 완료.

**배치15 QC (감독):**
- 00134565 이스타코 `1%미만` — `num()` 은 None (1.0 아님). 확인불가 12행은 삭제.
  ingest 는 미만/이상/해당없음 정성 표기를 행으로 남기지 않게 함. `qc.py` 에 `1%미만→None` 추가.
- 00121686 엠케이전자 부문매출 게이트 보류는 OFS/CFS 기지 패턴. 조치 없음.

**배치14 QC (감독):**
- 00152783 코메론 연혁 43행은 대체 표(상호변경·경영활동)에서 온 실제 이벤트. 규칙 파서가
  병합 셀 '사. 회사의 연혁'을 못 읽는 건 알려진 한계. 폴백이 채웠으니 파서는 당장 안 고침.
- 00153755 태양금속 부문매출 OFS/CFS 보류 — 기지 패턴. 조치 없음.
- 00145464 이구산업 시장점유율 2026A 24%는 원문 '(예상)'. 실적 아님 → 삭제.
  ingest 는 period_header 에 예상/전망/계획이 있으면 건너뛴다.

**배치17 QC (감독):**
- 00129387 SP삼화 시장점유율에 경쟁사(KCC·DPI·조광 등) 15행 — 산업 구성비. 당사(삼화페인트)
  3행만 남기고 삭제. 부문매출 '합계' 3행도 삭제.
- prepare 가 'I.회사의 개황'/'II.사업의 내용(제조업)' 을 현행 목차와 다르게 정확 매칭해
  0파일. `_find_section` 으로 개요/개황·사업의 내용 부분일치.
- 00136271 신성이엔지 부문합 2.47% 게이트 보류 — 강제 통과 안 함. 조치 없음.

**배치16 QC (감독):**
- 00140946 한솔 USA 2022A 21배 — 2021.11 신설 법인의 첫 온전한 해. 단위 오류 아님.
  게이트 보류는 유지(강제 적재 안 함). 2021A·2023A 는 들어가 있음.
- 00143226 스틸드럼 유통 2020A 0.1배 — 원문 천원 표기 그대로. 보류 유지.

폴백 14–17(최신 ok 42사) 종료. 연간 규칙 추출·주석 규칙은 전회차 목표로 계속.

**재현·수정:**
- `parse_segment_revenue` 단위 억원 하드코딩 → `detect_unit_scale()`. 코오롱글로벌
  부문합 9900% → 0.000001%. 삼성 회귀 동일.
- `parse_rd` 가 지주사 보고서의 `[주요 종속/자회사]` 표를 당사로 집어감.
  종근당홀딩스 경보제약 14.58조(당사 매출 0.88조). 당사 절은 '해당사항 없습니다'.
  그 표는 스킵. `--concepts rnd_total,rnd_revenue_ratio` 로 3행 제거. 삼성 회귀 동일.


## A. 데이터 오류 — 조치 필요
| 회사 | 문제 | 규모 | 제안 |
|---|---|---|---|
| 00111874 대원산업 | `market_share` 가 자사가 아닌 **국내 완성차 OEM 생산비중**(고객사 점유율). 합계 100% | 45행 | 삭제 또는 개념 재분류 — 사용자 판단 필요 |
| 00115612 동부건설 | `market_share` 를 **종속회사(동부엔지니어링)** 수치로 대체 | 확인필요 | 삭제 검토 |

→ 재발 방지: `/tmp/phaseB_prompt_common.md` 에 "누구의 점유율인가" 판정 규칙 추가함(배치 24~37 적용).

## B. 스코프 판단 — 감독 확인 필요 (지주·연결 대기업)
당사 사실만 채택하고 종속회사분을 제외한 판단. 게이트는 통과했으나 기준의 타당성 확인 필요.
- 00109693 DL㈜ — 연결합계가 fin_periods 와 0.000005% 일치(판단 뒷받침됨)
- 00109718 사조대림 — 종속회사 쪽 시장점유율 수십~수백건 제외
- 00111810 대웅 — 종속 3사(대웅제약·대웅바이오·한올바이오파마) 제품매출 제외

## C. Storage 원문 없음 (스킬 밖 — Phase 3 진행되면 해소)
00104856 삼성증권 · 00105952 LS · 00111722 미래에셋증권 · 00115694 DB증권
→ 전체 368개사 중 26개사가 이 부류(최신 회차 미보유). 측정 완료.

2026-08-26 재측정: 위 4사는 원문이 **생겼다**. 다만 최신 회차가 아니라 옛 회차다
(삼성증권 2018.12 · LS 2018.12 · 미래에셋증권 2021.12 · DB증권 2013.12).
Phase 3 후속 64개사 목록(`phase3-후속-대상.md`)에 포함. 최근 회차는 아직 없음.

## D. 게이트 보류 (정상 동작으로 보이나 확인 권장)
- 00104768 가온전선 특수케이블|상품 2025A — 10.8배 점프, 2024.10 ㈜지앤피 인수와 부합
- 00109310 대동기어 2행 · 00111689 2건 · 00111704 1건 · 00111838 1건
- 00115612 동부건설 기타부대사업 2024A 191억→2025A 10억

## E. 재검증 권장 (파싱 신뢰도)
- 00103644 24항목 연혁 — 날짜·내용이 각각 연결 문자열로 붕괴, 위치 1:1 매칭으로 복원(중신뢰)
- 00115676 · 00115931 연혁 37건·68건 — rowspan continuation 연도 상속 복원
- 00105271 KCC market_share — % 는 실재하나 기간 라벨이 없어 전량 스킵됨. 살릴지 판단

## F. 코드 결함 (미수정, 감독 처리 예정)
- OFS/CFS 대조 경로 부재 — 자회사 있는 회사에서 부문합 게이트가 구조적으로 실패
  (00103510·00103547·00103592·00106368·00106669·00107613·00108001·00108977 등 다수)
- `infer_period_labels` 앵커 오정렬 — 첫 컬럼 헤더 파싱 실패 시 전 기간 1년 밀림
  (batch02 실측. 헤더 2종은 수정했으나 근본 방어는 없음)

## G. ★ R&D 단위 스케일 결함 — 발견·수정 완료, DB 재적재 대기
`parse_rd()` 의 `unit_scale = 1_000_000` 하드코딩(주석: "표 단위: 백만원").
삼성 파일럿 표가 백만원이라 통과했으나 **대부분의 회사는 천원**을 쓴다.
→ R&D 금액이 정확히 1000배로 적재됨. 실측: 보령 46.3조원(실제 558억, 매출 8,596억).
→ 전수 검사: **72개사 232행에서 R&D > 매출**(물리적 불가능). rnd_total 전체의 42%.

**수정**: `detect_unit_scale()` 신설 — 표 앞 `(단위 : 천원)` 표기를 읽어 배수 결정.
표기를 못 찾으면 **배수를 지어내지 않고 금액을 확인불가로** 남긴다(status에 사유).
검증: 보령 558억(매출의 6.5%) · 삼성 '백만원, %'→×1e6 정확 검출 ·
삼성 회귀 블록별 수치 완전 동일 · R&D비중 게이트 6기간 전부 0.05pt 이내 통과.

**남은 조치**: 수정 전에 적재된 행이 DB에 그대로 있다. 전 배치 종료 후
`/tmp/rnd_unit_fix_targets.txt` 의 회사들에 `extract` 재실행(스코프 교체라 덮어써짐).
규칙 파서만 도는 것이라 회사당 ~1초.

**게이트 설계 교훈**: R&D비중 재계산 게이트가 1000배 불일치를 감지했는데도 **비율만
막고 금액은 통과**시켰다. 재계산 게이트가 실패하면 그 입력인 금액도 함께 보류해야 한다.

## H. ★ 종속회사 앵커 오적용 — 발견·수정 완료 (독립 2건 재현)
`infer_period_labels()` 가 "표 직전의 가장 가까운 '2025년(제N기)' 문장"을 앵커로 쓰는데,
대형 그룹사 보고서에는 **종속회사의 앵커 문장이 섞여** 있어 주체를 구분하지 못한다.

실측 2건(서로 다른 배치가 독립 발견):
- 00113526 대한항공 — 종속 한국공항㈜ '제59기' 앵커 → segment_revenue 가 **2028A~2030A**
- 00111999 대원제약 — 종속 에스디생명공학 '제18기' 앵커 → **2072A/2071A/2070A** (47년 오차)
둘 다 해당 배치가 자체 롤백했고, 전 DB 스캔 결과 **미래 연도 잔존 0행** 확인.

**수정**: 산출 연도가 보고서 회계연도를 넘으면 그 열을 버린다(연도를 지어내지 않는다).
정기보고서 실적 표는 회계연도를 넘는 열을 가질 수 없으므로 이 조건은 안전하다.
검증: 두 회사 모두 미래연도 0건 · 삼성 회귀 완전 동일.

**한계**: 오차가 미래로 튀지 않는 방향(과거로 밀림)은 이 방어가 못 잡는다. 부문합 게이트가
2차 방어선이지만(엉뚱한 연도 매출과 대조되어 대개 실패) 완전하지 않다. 근본 해결은
앵커 문장의 **주체가 당사인지** 판정하는 것 — 종속회사명 목록과 대조하는 방식이 후보.

## I. 기간 라벨 변형 — 누적 수정 (배치들이 실행 중 계속 발견)
`parse_period_col()` 이 인식 못해 **완성된 데이터가 통째로 0행 처리**되던 형식들.
수정 후 22케이스 전수 통과(받을 18종 + 거부할 4종), 삼성 회귀 동일.

받게 된 것: `제N기(YYYY년)` `제N기(YYYY년도)` `제N기(FY YYYY)` `제N기 연간(YYYY년도)`
`제N기(당)` `제 N(당) 기` `제N기 연간` `제N기 당기/전기/전전기` `FY2025`
`YYYY년도` `YYYY연도` `YYYY년(제N기)` `YYYY년도(제N기)` `YYYY년 당기` `YYYY년(1.1~12.31)`

**의도적으로 계속 거부**: `YYYY누계`(사업보고서=연간 / 분기보고서=YTD 구분 불가) ·
`제N기 당분기`(분기 번호 없음) · `당기`(연도 정보 없음) · `소계`

**재실행 후보** (라벨 결함으로 데이터를 잃은 회사, 수정 후 회수 가능):
- 00113225 대한제강 — segment_revenue 전량 ('2025누계', 여전히 거부 대상이라 회수 불가)
- 00110750 — segment_revenue 0행 (`제N기(YYYY년도)`) → 회수 가능
- 00131896 market_share 6건 · 00132202/00132318/00132354 segment_revenue 88건 → 회수 가능
- 00121288 · 00122472 · 00122481 · 00113508 · 00113535 · 00130587 → 회수 가능
- SK증권(`YYYY년(1.1~12.31)`) · 서원(`YYYY년 당기`) → 회수 가능

## J. 1990~1992년 오적재 3행 (과거 방향 앵커 오류)
00108977 대구백화점, 종속회사 표(대백아울렛)에서 `1990A/1991A/1992A` 각 1행, 값이
3년 모두 55억으로 동일 — 앵커 오류의 명백한 증거. 미래연도 방어가 못 잡는 방향이다.
→ 삭제 대상. 커밋 시 정리.

## K. 회사당 확보 연수 (실측)
segment_revenue 252개사 중앙값 **3년** · rnd_total 169개사 3년 · market_share 102개사 3년
shareholding_pct 353개사 1년(시점 개념) · corp_history 285개사 이벤트 중앙값 17건
연도 분포: 2025년 1,548행 · 2024년 1,497행 · 2023년 1,429행
**회사당 사업보고서 1건만 사용**(353개사 중 351개사) — 표가 3개년을 담아 자동으로 3년치.
더 긴 시계열은 과거 회차 추가 실행으로 가능(원문이 Storage 에 있어 DART 호출 0건).

## L. ★ 게이트 스킵 = 미검증 적재 (설계 구멍, 정량화 완료)
부문합 게이트는 `fin_periods.revenue` 와 대조하는데, **그 값이 NULL 이면 게이트가 스킵되고
데이터는 그대로 적재된다.** "스킵"과 "통과"가 같은 결과를 낳는다.

실측: segment_revenue 4,881행 중 **250행 / 22개사 (5.1%)** 가 대조 없이 적재됨.
보험사만이 아니다 — 케이씨씨·대교·동국제약·동진쎄미켐·벽산 등 일반 제조사도 포함
(해당 기간이 fin_periods 에 없거나 revenue 가 안 채워진 경우).
대표 사례: 삼성화재 42행(전 연도 revenue NULL, 보험사는 수입보험료 개념) —
값 자체는 자릿수상 그럴듯하나 **검증된 바 없다.**

**제안**: status 에 '미검증(대조불가)' 를 명시해 적재하거나, 대조 불가 시 보류로 바꾼다.
지금은 통과분과 구분이 안 돼 downstream 이 신뢰도를 판단할 수 없다.

## M. ★★ 감독 실수 — 부분 재적재가 에이전트 산출물을 파괴 (사고·복구 완료)
R&D 단위 결함을 고치려고 161개사에 `extract` 를 재실행했다. 그런데 `load_scope()` 는
**ALL_CONCEPTS 전체에 replace_scope** 를 건다(스테일 행 제거 목적). 규칙 파서는 연혁·
부문별매출·시장점유율을 0행으로 내므로, 에이전트가 채운 그 블록들이 빈 리스트로 덮어써졌다.

  fin_details 10,412 → 7,593 (-2,819) · corp_history 7,703 → 4,191 (-3,512, 336→190개사)

**원인**: "스테일 행 제거"는 규칙 단독 운영을 전제한 설계인데, 지금은 같은 스코프에
에이전트 산출물이 공존한다. 감독이 이 동작을 알면서도 회사 1건 검증 시 R&D 결과만 보고
다른 블록이 비워지는 것을 확인하지 않았다.

**복구**: `/tmp/phaseB_fallback/*/*.ingest.json` 352개가 남아 있어 160/161 개사를
`llm_fallback.py ingest` 로 재적재. 최종 fin_details 10,661 · corp_history 7,878 로
사고 전보다 많다(R&D 회수분 포함). 무결성 전수 확인: 이상 period_key 0 · event_ym NULL 0 ·
R&D>매출 0.

**재발 방지**: `extract --concepts <목록>` 신설. 지정하면 그 concept 만 교체하고 나머지
concept 과 corp_history 는 건드리지 않는다. 실측 검증(보령): rnd_total·rnd_revenue_ratio
만 교체하고 연혁 25행·부문매출 91행·시장점유율 2행 전부 보존됨.

**교훈**: 이 사고가 복구 가능했던 유일한 이유는 에이전트 산출물이 파일로 남아 있었기
때문이다. 위 "기록의 한계"(보고서가 /tmp 에만 존재)는 취약점이자 이번엔 생명줄이었다.

## N. `qc.py integrity` 가 새로 찾아낸 시장점유율 개념 오류 (미조치)
하네스를 만들고 돌리자 감독이 손으로 찾은 것(대원산업) 외에 두 유형이 더 나왔다.

| 회사 | 축 | 내용 | 판정 |
|---|---|---|---|
| 00102858 고려아연 | 금·은·연·아연·전기동 | 내수비중 84% / 수출비중 16% | **개념 오류** — 점유율이 아니라 자사 매출의 내수·수출 구성비. `market_share` 가 아니다 |
| 00105299 금강공업 | 사료 | 고려산업 2.5% (종속회사) | 출처표에 "(연결기준, 고려산업)" 명시. 당사 점유율은 아님 |
| 00111874 대원산업 | 생산비율·판매비율 | 완성차 OEM 구성비 | **오적재**(기존 발견) |

셋 다 삭제/재분류 판단이 필요하다. 고려아연은 개념 자체가 다르므로 `market_share` 에서
빼는 것이 명확하고, 나머지 둘은 "종속회사·고객사 점유율을 어떻게 취급할지" 정책 결정이다.
