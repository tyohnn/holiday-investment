# 투자 리서치 플랫폼 로드맵

> 2026-07-24 기준. 강의 155편 → 노트 146편 → 교재 56챕터로 구조화된 지식 자산을 코어로,
> "딸깍 한 번"에 기업·산업·거시 리서치가 나오는 개인 리서치 플랫폼을 만든다.

## 딸깍 액션 (사용자 관점)

| # | 액션 | 실행 주체 | 상태 |
|---|---|---|---|
| 1 | "이 기업 기본 분석해줘" | `company-analysis` 스킬 — 기본 모드 | ✅ 2026-07-24 |
| 2 | "이 기업 심층 분석해줘" (산업·밸류체인까지) | `company-analysis` 스킬 — 심층 모드 | ✅ 2026-07-24 |
| 3 | "이 산업 심층 분석해줘" | `industry-analysis` 스킬 (8단계 탑다운 프레임) | ✅ 2026-07-24 (초판) |
| 4 | "이 섹터 기본 분석해줘" | `sector-scan` 스킬 (간이 PER·PSR·저평가 사다리 스크리닝) | 예정 |
| 5 | "거시경제 트래커 켜줘" (매일 보고서) | `macro-tracker` 스킬 + 스케줄 실행(cron) | 예정 |

> 스킬은 **`plugin/`** 자립형 플러그인에 담긴다 (Claude Code·Codex·Cursor 배포).
> 방법론은 `skills/*/` 안에 self-contained. `교재/`는 저자가 유지하는 지식 원천이고, 플러그인은
> 그것을 증류한 별도 제품이다 — 플러그인은 `교재/`를 참조하지 않는다.

## 구성요소 6

1. **차트 컴포넌트 큐레이션** — shadcn charts·Recharts·TradingView 위젯 기반. 믹스패널급 대시보드 차트 + 주식투자자용 차트(캔들·밸류체인 지도·시나리오 매트릭스 히트맵)를 고퀄리티로 큐레이션해 두고, 리포트·대시보드가 재사용. 유저가 에이전트와 매번 새로 만들 필요가 없도록 `apps/web`에 컴포넌트 라이브러리로 정착.
2. **영상 분석 플러그인** — yt-dlp(스킬 보유)·ffmpeg 프레임 캡처·YouTube Data API 검색, 그리고 이들을 묶는 스킬. 기존 `scripts/` 전사 파이프라인이 원형. 이 레포 밖에서도 쓸 수 있게 범용 플러그인으로 추출.
3. **DART 공시 파서 플러그인** — 전자공시 원본(PDF·XLSX)을 에이전트가 읽기 쉬운 MDX로 변환하는 CLI + 스킬 (claude 기본 pdf·xlsx 스킬과 조합).
4. **분석 방법론 모음** — 교재① 방법론(= `company-analysis` 스킬)이 하우스 방법론 1호. 이후 투자 구루별·이론별 모듈(버핏·피셔·린치·켄 피셔 등 — 교재에 이미 원전이 녹아 있음)을 추가.
5. **애널리스트(트래커) 스킬 모음** — ①기업 트래킹(매일 뉴스·공시·영상·정책 수집→분석, 나만의 애널리스트) ②산업 트래킹(탑다운) ③거시경제 ④외교안보 ⑤정책. cron 스케줄과 결합해 데일리 리포트 생성.
6. **대시보드** — `apps/web`(Fumadocs) 확장. `리서치/` 리포트를 rich component MDX로 렌더링. 산업 밸류체인 지도, 뉴스 페이지, 시뮬레이션(9칸 매트릭스 인터랙티브) 페이지.

## 저장 컨벤션

- 분석 산출물: `리서치/기업/<종목>/분석/YYYY-MM-DD-{기본|심층}분석.md`,
  `리서치/산업/<산업>/분석/YYYY-MM-DD-산업분석.md`
  — frontmatter 스펙은 [report-templates.md](plugin/skills/company-analysis/references/report-templates.md)
- 종목/산업 폴더 안은 역할별 분리: `자료/{재무,공시,IR,뉴스,유튜브}` = raw 수집 자료(출처
  frontmatter 필수), `분석/` = 완성 리포트, `계산/` = assumptions·valuation JSON — 상세는
  [data-layout.md](plugin/skills/company-analysis/references/data-layout.md). 시세는 변하는
  값이라 raw 로 쌓지 않고 assumptions.json 에 기준일과 함께 기록. 트래커(P3)가 같은
  `자료/`에 데일리 수집을 쌓는다.
- 계산·검증은 스크립트가 강제: `valuation.py`(9칸 계산기), `validate_report.py`(저장 전 게이트,
  exit 0 필수). 순수 stdlib Python 3.9+, pip 불필요.
- 수집도 스크립트가 우선: `dart.py`(OpenDART 공시·재무 원본 — DART_API_KEY 무료 발급),
  `fetch_news.py`(Google News RSS 목록), `fetch_youtube.py`(yt-dlp 자막→타임스탬프 md, 심층
  전용). 에이전트는 선별·발췌만. 키·yt-dlp 없으면 웹서치 폴백/스킵으로 완주.
- 향후: `리서치/섹터/`, `리서치/거시/`
- 스킬: **`plugin/skills/<name>/`** 에 self-contained 로 커밋. 새 스킬(sector-scan·
  macro-tracker 등)도 여기에 추가하고 세 매니페스트(`.claude-plugin/plugin.json`의 `skills`
  배열, `.cursor-plugin/plugin.json`·`.codex-plugin/plugin.json`의 `skills` 경로는 `./skills/`
  라 자동 인식되므로 보통 수정 불필요)에 등록한다. 방법론은 스킬 폴더 안에서 완결(외부 `교재/`
  참조 금지).

## 단계

- **P1 (2026-07-24 완료)**: `plugin` — self-contained 플러그인화. `company-analysis`(기본+심층)
  ·`industry-analysis` 스킬, 리포트 컨벤션 확정. 교재 방법론을 스킬 안으로 증류(외부 참조 제거).
  3종목 검증 완료. 이후 Claude Code·Cursor·Codex 각각의 **공식 네이티브 플러그인 매니페스트**
  (`.claude-plugin/`·`.cursor-plugin/`·`.codex-plugin/`)로 재정의 — 세 도구 모두 `skills/`
  폴더를 그대로 자동 인식해서, 파일을 복사·치환하던 어댑터 스크립트 없이 매니페스트 3개만으로
  세 도구에 배포된다.
- **P2**: `sector-scan` 스킬 (간이 PER·PSR·저평가 사다리 스크리닝) + `industry-analysis` 실전 보강.
  같은 플러그인에 스킬 추가하는 패턴 확립.
  - E2E 피드백 백로그 (2026-07-24 크래프톤 심층 런에서 도출, 우선순위순):
    ①`dart.py peers <회사들>` — 경쟁사 재무 일괄 + 시총 웹 보강 (심층 상대가치의 도구 공백)
    ②annual INDEX 체크리스트에 주석 라인 앵커 자동 첨부 (주석 4천줄 grep 비용 절감)
    ③`valuation.py` 부문별 매출비중·이익률 배열 → 가중평균 옵션 (믹스 변화 기업)
    ④`validate_report.py` 9칸 셀 값을 valuation.json과 실제 대조 (현재는 형태만 검사)
    ⑤`fetch_youtube.py` 자막 고유명사 치환 사전 (회사명·제품명 오전사 보정)
- **P3**: 트래커 스킬들 + 스케줄 실행 (거시 데일리 브리핑부터), 기업 트래커는 `리서치/기업/<종목>/트래킹/`에 누적
- **P4**: DART 심화(원본 PDF/XLSX→MDX 변환 — 목록·재무 API는 P1.5의 dart.py로 선반영됨),
  차트 컴포넌트 라이브러리, 대시보드 렌더링 (리서치 리포트 → MDX 자동 변환)

## 플랫폼 실행 계획 (2026-07-25 확정)

> 전략: **정형 데이터를 먼저 전량 수집해 최신식 UI·차트로 보여주고, 에이전트 비정형 분석은
> 소수 관심기업만 시드로 채운 뒤, 고객이 크레딧(자기 비용+마진)으로 에이전트 분석을 생성하게
> 해서 매출과 데이터 축적이 동시에 일어나는 플라이휠**을 만든다. 그 전에 로컬에서 종목
> 단위로 시행착오를 겪으며 데이터 모델과 플랫폼 원자(종목 페이지)를 확정한다.

### P-A. 로컬 파일럿 — 종목 2개로 전체 구조 깎기 (~2주)

- **종목**: 크래프톤(기존 자산 재활용, 깨끗한 케이스) + 에코프로비엠(유증·물적분할·소송·
  지주관계 — 스키마 단련용). 두 종목이니 쿼터 부담 없이 **전 역사** 수집.
- **정형 저장소**: Supabase local(docker) + `supabase migration`으로 스키마 버전 관리.
  Postgres라서 P-B 승격이 `db push`로 끝나고, P-C에서 Auth·RLS·PostgREST를 그대로 쓴다.
- **A1 정형 — ingest 파이프라인**: dart.py 산출 → Postgres 적재. 스키마 v0 초안:
  `companies · filings(메타+정정 체인) · financial_facts(계정 행) · report_items ·
  events(주요사항) · ownership_txns(지분거래) · trackings(사실 시계열 행) · analyses(판정 이력)`.
  md는 버리지 않는다 — **DB가 정형 정본, md는 에이전트 인터페이스**이고 ingest가 md→DB
  단방향 동기화한다(스킬 무수정). 시행착오(기재정정 표현, 계정명 변형, 액면분할 연속성)를
  마이그레이션 파일로 축적하는 것이 이 단계의 산출물이다.
- **A2 비정형 — 2회차 갱신 사이클 검증**: 지금까지는 전부 1회차(백지)만 검증했다.
  신규 공시 → 증분 수집(manifest) → 트래킹 append → "추정의 갱신" 리포트까지, 플랫폼
  운영 모드의 원형을 두 종목에서 돌린다.
- **A3 UI — 플랫폼 원자 "종목 1페이지"**: Next.js + Supabase 로컬. 재무 차트(shadcn
  charts — 구성요소 1의 시작)·공시 타임라인·트래킹 시계열 뷰·9칸 매트릭스. 이 페이지가
  확정되면 전 종목 확장은 복제다.
- **완료 기준**: ①두 종목 전 역사가 스키마 변경 없이 멱등 재적재 ②기재정정이 DB에서
  표현·조회됨 ③트래킹 사실이 정해진 정본 형태로 존재(아래 유보 사항 ⑵의 결정 반영)
  ④종목 페이지가 DB만 읽어 렌더 ⑤수집 단가·시간 실측치 갱신.

### P-A 설계 유보 사항 — 확정이 아니라 가설이다

위의 스키마 초안·폴더 구조·"DB 정본 + md 인터페이스" 원칙까지 전부 **A1·A2를 돌리며
계속 판단하고 바꾼다.** 파일럿의 목적 자체가 모델을 깎는 것이므로, 변동은 실패가 아니라
산출물이다. 변동 이력은 마이그레이션 파일과 git이 담고, md 레이아웃을 바꿀 때는 기존
산출물 이행 스크립트를 함께 만든다(기존 4종목 + 파일럿 2종목이 이행 대상).

**⑴ 폴더 구조**: 자료/트래킹/계산/분석 4계층과 이름 규칙(docs/리서치-데이터-설계-원칙.md)은
현재 가설. DB가 정본이 되는 항목이 늘수록 md 쪽 구조는 단순해질 수 있다.

**⑵ 트래킹(사실 시계열) 저장 형태 — 아직 정답을 모르는 문제.** 후보 3안:

| 안 | 형태 | 강점 | 약점 |
|---|---|---|---|
| A. 주제별 md 문서 (현행) | `트래킹/<주제>.md`에 표로 append | 에이전트가 읽고 쓰기 쉬움, 사람이 읽힘, git diff | 크로스 종목 쿼리 불가("최근 90일 유증 기업 전부"), UI가 md 파싱 필요, 사실 단위 메타(중복 키·신뢰도) 부착 어려움 |
| B. DB 사실 원장(facts 테이블)이 정본, md는 생성된 뷰 | 행 = (종목, 주제/태그, 시점, 사실, 수치, 출처, 접수번호) | 플랫폼 네이티브 — UI·스크리너 직결, append-only 원장이 "수정 금지" 원칙과 일치, 중복 키 가능 | 에이전트가 행 삽입 도구를 거쳐야 함(valuation.py처럼 스크립트 강제로 해결 가능), md 뷰 재생성 필요 |
| C. 하이브리드 — md 정본 + ingest가 파싱해 DB 동기화 | 현 계획의 초안 | 스킬 무수정 | md는 스키마가 아니라 파싱이 취약, 정본이 둘로 갈라질 위험 |

직감은 B(원장)로 수렴할 가능성이 높지만, **A2에서 실증으로 판정한다.** 판정 기준:
①에이전트가 사실을 추가할 때의 마찰(2회차 갱신 런에서 측정) ②같은 사실이 공시와 뉴스
양쪽에서 올 때의 중복 처리 ③종목 페이지가 시계열을 그릴 때의 비용 ④크로스 종목 쿼리
요구(P-B 스크리너 가정) ⑤기재정정을 사실 원장에서 표현하는 방식.

**⑶ 열린 질문들**: 주제가 폴더/파일 단위 분류여야 하나, 태그여야 하나(유증 결정은
자금조달이면서 지배구조다) · 사실의 중복 제거 키(접수번호+항목?) · 출처 등급(공시 확정
vs 언론 보도)의 표현 · 트래킹과 정형 events 테이블의 중복 범위(정형 공시에서 나온
사실은 원장에 복제하나, 참조하나).

### P-B. 정형 전량 + 플랫폼 MVP (4~8주)

- 파일럿 스키마를 Supabase 호스티드로 승격, 키 3~5개로 전 상장사 백필(~1주).
- 전 종목 스크리너 + 종목 페이지 복제 + 차트 라이브러리 정착.
- 비정형은 **관심 유니버스 20~50종목만 우리 비용으로 시드** — "트래킹까지 있는 종목"이
  크레딧 판매의 데모가 된다. 트래커(P3)는 이 유니버스에 먼저 붙인다.

### P-C. 고객 + 크레딧 플라이휠

- 크레딧 구매 → 에이전트 잡(심층 분석·트래킹 생성) → 산출물 플랫폼 귀속. 원가 실측
  (심층 1건 ~$2~8) 위에 마진 설계.
- 착수 전 결정 2건: 생성 데이터 공유 모델(첫 구매자 부담 후 공개? 기여 리워드?),
  신선도 정책(갱신 크레딧). 확인 1건: 유료 분석 판매의 유사투자자문업 등 규제 검토 —
  "종목 추천이 아닌 방법론 문제풀이 + 사실 데이터" 프레이밍 유지가 유리.

기존 P2~P4와의 관계: P-A/P-B가 P4(대시보드·차트)와 구성요소 1·6을 앞당겨 실행하는
것이고, P3(트래커)는 P-B의 관심 유니버스에 먼저 적용한다. P2(sector-scan)는 플랫폼
스크리너로 흡수될 수 있어 우선순위를 P-A 뒤로 미룬다.

## 플랫폼 스케일업 추정 — 전 상장사 공시 데이터화 (2026-07-25 분석)

전 코스피·코스닥 상장사의 공시를 정형 데이터(+Knowledge Graph)로 만드는 시나리오의
타당성 추정. 지금의 dart.py·manifest·트래킹 규칙이 그대로 파이프라인 단위 부품이 된다.

- **모수 (list API total_count 실측, 2026-07-25)**: 상장사 공시 연 12.1만 건(2025),
  최근 5년 누적 ≈ 55만~60만 건, 전자공시 전 역사(2000~) ≈ 230만 건. 건수의 절반 가까이는
  지분공시라 이미 정형 API 영역. 모수는 콜 몇 번으로 언제든 재실측 가능.
- **정형 파트 (에이전트 불필요, rule-based ETL)**: 병목은 OpenDART 쿼터(키당 일 2만 콜)
  하나. 공시 메타 전 역사 2.3만 콜, 재무제표 전계정(2015~) 15만 콜, 정기·주요사항·지분
  (5년) 32만 콜, 사업보고서 원본(5년) 5.4만 콜·~300GB → **키 3~5개면 백필 1주 내**.
  진짜 시간은 개발: 파이프라인(큐·재시도·기재정정 반영) 1~2주 + KG 스키마·엔티티 해석
  (임원 동명이인·비상장 관계사 매칭) 2~4주. 주요사항 API는 필드가 이미 구조라 노드/엣지
  변환도 rule-based.
- **비정형 파트 (에이전트 필수)**: E2E 실측(심층 1사 33.6만 토큰) 외삽 — 트래킹 구축만이면
  사당 30만~80만 토큰. 전 상장사 5년 초기 구축 **$5k~15k(약 700만~2,000만 원), 병렬 100이면
  벽시계 2~5일**. 증분 운영은 일 신규 공시 ~480건 중 LLM 필요분 ~20% → 월 20만~60만 원.
- **티어링 (뉴스·유튜브 비용 통제)**: 전 상장사 = 공시 기반 자동 트래킹만 / 관심 유니버스
  (수십~수백) = +뉴스·유튜브·주석 심층 daily / 요청 종목 = +심층 리포트 온디맨드.
- **종합**: 정형 플랫폼 v1까지 1.5~2개월(개발 3~6주 ∥ 백필·트래킹 구축 병행), 초기 LLM
  비용 1~2천만 원 선. 배경 분석 상세는 세션 기록, 데이터 설계 원칙은
  [docs/리서치-데이터-설계-원칙.md](docs/리서치-데이터-설계-원칙.md).
